# Codex 工作规则

- 只能修改 `/workspace` 内文件。
- 输入素材位于 `/media/input`，默认只读取，不改写原始素材。
- 输出视频必须写入 `/media/output`。
- 默认 composition id 是 `MainVideo`。
- 默认渲染命令是 `npm run render`。
- 不要把密钥、token、环境变量或登录凭据写入源码、日志或视频输出。
